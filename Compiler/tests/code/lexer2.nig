#include "stdnigel.nigh"

fn byte main() {
	byte a = -2;
	byte b = 7;
	
	u8 sum;
	u8 avg;
	sum = a+b;//Add a to b
	outp0(sum);
	
	if( sum % 2 == 0 ) {
		avg=sum /2;/*Calculate the average /*nested comment*/ value*/
		outp2(avg);
	}
	else {
		outp2(0);
	}
	
	return 0;
}